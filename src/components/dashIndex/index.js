const e = React.createElement;

export default class dashIndex extends React.Component {
    render() {
        return (
            e('section', { className: 'dash-index' },
                null
            )
        )
    }
}